hello!!
the relevent code can be found in ChatGPTManager.cs 

the other .cs files were used for version control/history.
i kept them in case you wanted to look at them, but they're essentially all the same with minor changes that accumulated over time